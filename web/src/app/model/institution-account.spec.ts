import { InstitutionAccount } from './institution-account';

describe('InstitutionAccount', () => {
  it('should create an instance', () => {
    expect(new InstitutionAccount()).toBeTruthy();
  });
});
